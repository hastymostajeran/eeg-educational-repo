# Repository Notes

The primary notebooks are stored as `.ipynb` files in `notebooks/`.

For users who prefer the requested `# %%` notebook-cell format, equivalent percent-format source files are available in:

```text
docs/notebook_percent_scripts/
```

These files can be opened directly in VSCode or PyCharm as notebook-style Python scripts.
